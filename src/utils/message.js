const validate = {
    bodyValidateFail: 'body-validate-fail'
}

const auth = {
    userNotFound: "user-not-found",
    passwordError: "username-or-password-is-invalid"
}

export const message = {
    validate,
    auth
}